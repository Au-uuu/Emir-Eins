# 2026-09-22 收紧斜杠：仅 添加/删除/来只 可省略

## 需求

- `图库`、`关联`（含 `取消关联`、`查找关联`）、`私有`、`公开` **必须带 `/`**。
- 帮助里写清楚：**只有** `添加 / 删除 / 来只` 可省略斜杠（`添加`、`删除` 需引用图片）。
- 帮助里补上 `/图库 统计`。

## 改动

正则从 `^/?xxx` 改为 `^/xxx`（必须带斜杠）：

| 指令 | 之前 | 现在 |
|---|---|---|
| 图库 | `^/?图库` | `^/图库` |
| 关联 | `^/?关联` | `^/关联` |
| 取消关联 | `^/?取消关联` | `^/取消关联` |
| 查找关联 | `^/?查找关联` | `^/查找关联` |
| 私有 | `^/?私有` | `^/私有` |
| 公开 | `^/?公开` | `^/公开` |

仍可省略斜杠（不变）：`添加`、`删除`、`来只`。

`/批量添加` 之前已要求斜杠。

### 帮助文案

- 新增 `/图库 统计　统计信息`。
- 末尾新增一行快捷规则：
  > 快捷：只有「添加 / 删除 / 来只」可省略斜杠（添加、删除需引用图片），
  > 　　　其余指令必须带「/」。

## 改动文件

- `bot.py`：6 个正则改为 `^/`；`HELP_TEXT` 更新。
- `_test_commands.py`：新增「裸指令被无视」与帮助文案断言。
- `README.md`：斜杠规则说明更新。

## 测试

9 项测试全绿。

## 备注

- 裸 `图库` / `关联 …` 等现在会被当作普通消息**静默忽略**（群 @ / 单聊策略一致）。
- 仍保留的免斜杠别名（调试用，不在帮助里宣传）：`帮助/菜单`、`在线`、`复读`。
  如需一并收紧可再说。
