# 2026-09-22 移除 `/图库 -k`

## 需求

`/图库` 已经能列出图库（按图片数降序、带关联词、可翻页），`/图库 -k`（纯关键词＋数量）
信息更少且重复，去掉。

## 改动

- `bot.py`：`do_gallery` 删除 `-k` / `关键词` / `keys` 分支。
- `README.md`：指令表去掉该行。
- `_test_commands.py`：去掉 `/图库 -k` 用例。

## 备注

- `ImageStore.list_keywords` 仍保留（`_test_store.py` 在用），只是不再有对应指令。
- 现在 `/图库 -k` 会落到「关键词查询」分支，返回「关键词『-k』下没有图片」。
- `/图库 统计`（`-s` / `stats`）保留。

## 测试

9 项测试全绿（`_test_commands.py` 已更新）。
